# API keys and tokens — copy to .secrets and fill in values
# This file is gitignored. Set permissions: chmod 600 .secrets

# Required: Hugging Face token for downloading models
HF_TOKEN=

# Optional: xAI API key for complex/enrich routing (omit to disable)
XAI_API_KEY=

# Optional: API key for authenticating external clients (e.g. Open WebUI)
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=

# Optional: Cloudflare Tunnel token for secure ingress (omit to disable)
# See docs/cloudflare-tunnel-setup.md for setup instructions
CF_TUNNEL_TOKEN=
