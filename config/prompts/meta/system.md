You are processing a structured task about a prior conversation.
Follow the task instructions exactly. Be concise. If a specific output
format is requested (e.g. JSON), return only that format with no extra text.