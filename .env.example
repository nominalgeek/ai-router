# Timezone for timestamps in logs and date injection into prompts
# Default: America/Los_Angeles (US Pacific)
TZ=America/Los_Angeles

# Optional: xAI search tools for enrichment (comma-separated)
# Available tools: web_search, x_search
# Set to empty string to disable search (Grok will answer from training data only)
# Default: web_search,x_search
XAI_SEARCH_TOOLS=web_search,x_search

# Optional: write session log JSON files for meta-pipeline requests
# (title generation, follow-up suggestions, summaries from Open WebUI)
# Default: false (meta sessions are logged to app.log but no JSON file is written)
# LOG_META_SESSIONS=false

# Optional: xAI model selection
# Available: grok-4-1-fast-reasoning, grok-4-1-fast-non-reasoning, grok-code-fast-1
# Default: grok-4-1-fast-reasoning
# XAI_MODEL=grok-4-1-fast-reasoning
